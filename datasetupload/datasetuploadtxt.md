# user can upload a dataset <200mb as csv , xlsx (streamlit).
library - streamlit

function - file_uploader("upload csv, xlsx" , type =["csv","xlsx"])

function takes file with specified file type as a input . 

At the end of this we have a file inside our system which is yet to clean