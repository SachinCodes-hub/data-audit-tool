# Data Audit Tool

100% local. No API keys. No internet required.

## Run

```bash
pip install -r requirement.txt
streamlit run ui.py
```

Then open `index.html` with Live Server.
Click "Launch App" — Streamlit opens embedded on the same page.
